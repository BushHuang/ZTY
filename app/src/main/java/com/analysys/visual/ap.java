package com.analysys.visual;

public interface ap {

    public enum a {
        NOT_YET_CONNECTED,
        CONNECTING,
        OPEN,
        CLOSING,
        CLOSED
    }

    public enum b {
        CLIENT,
        SERVER
    }

    void a(bl blVar);
}
